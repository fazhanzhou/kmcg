<?xml version="1.0" encoding="UTF-8"?>
<configuration  scanPeriod="60 seconds" debug="false">

    <contextName>kmcg</contextName>

    <!--日志保存目录-->
    <property name="LOG_PATH" value="logs/kmcg"/>
    <!--程序名称-->
    <property name="appName" value="kmcg"/>
    <!--日志最大保存天数-->
    <property name="maxSaveDays" value="365"/>
    <!--单个文件最大大小-->
    <property name="maxFileSize" value="200MB"/>
    <!--日志最大保存上限-->
    <property name="totalSizeCap" value="20GB"/>

    <!--输出到控制台 ConsoleAppender-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%contextName %d{"yyyy-MM-dd HH:mm:ss,SSS"} [%thread] [%-5p] [%c{0}-%M] - %m%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>debug</level>
        </filter>
    </appender>

    <appender name="infoLogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--如果只是想要 Info 级别的日志，只是过滤 info 还是会输出 Error 日志，
        因为 Error 的级别高，所以我们使用下面的策略，可以避免输出 Error 的日志-->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!--过滤 Error-->
            <level>Error</level>
            <!--匹配到-->
            <onMatch>DENY</onMatch>
            <!--没有匹配到-->
            <onMismatch>ACCEPT</onMismatch>
        </filter>
        <!--日志名称，如果没有File属性，那么只会使用FileNamePattern的文件路径规则
            如果同时有<File>和<FileNamePattern>，那么当天日志是<File>，明天会自动把今天的日志改名为今天的日期。
            即，<File> 的日志都是当天的。
        -->
        <File>${LOG_PATH}/${appName}-log-info.log</File>
        <!--滚动策略，按照时间滚动 TimeBasedRollingPolicy-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--文件路径,定义了日志的切分方式（把每一天的日志归档到一个文件中,以防止日志填满整个磁盘空间）-->
            <fileNamePattern>${LOG_PATH}/${appName}-log-info-%d{"yyyy-MM-dd"}.%i.log.zip</fileNamePattern>
            <!--只保留最近365天的日志-->
            <maxHistory>${maxSaveDays}</maxHistory>
            <!--单个文件最大大小-->
            <maxFileSize>${maxFileSize}</maxFileSize>
            <!--用来指定日志文件的上限大小，那么到了这个值，就会删除旧的日志-->
            <totalSizeCap>${totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <!--日志输出编码格式化-->
        <encoder>
            <pattern>%contextName %d{"yyyy-MM-dd HH:mm:ss,SSS"} [%thread] [%-5p] [%c{0}-%M] - %m%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>


    <appender name="errorLogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--如果只是想要 Error 级别的日志，那么需要过滤一下，默认是 info 级别的，ThresholdFilter-->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>Error</level>
        </filter>
        <!--日志名称，如果没有File属性，那么只会使用FileNamePattern的文件路径规则
            如果同时有<File>和<FileNamePattern>，那么当天日志是<File>，明天会自动把今天的日志改名为今天的日期。
            即，<File> 的日志都是当天的。
        -->
        <File>${LOG_PATH}/${appName}-log-error.log</File>
        <!--滚动策略，按照时间滚动 TimeBasedRollingPolicy-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--文件路径,定义了日志的切分方式（把每一天的日志归档到一个文件中,以防止日志填满整个磁盘空间）-->
            <fileNamePattern>${LOG_PATH}/${appName}-error-info-%d{yyyy-MM-dd}.%i.log.zip</fileNamePattern>
            <!--只保留最近365天的日志-->
            <maxHistory>${maxSaveDays}</maxHistory>
            <!--单个文件最大大小-->
            <maxFileSize>${maxFileSize}</maxFileSize>
            <!--用来指定日志文件的上限大小，那么到了这个值，就会删除旧的日志-->
            <totalSizeCap>${totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <!--日志输出编码格式化-->
        <encoder>
            <pattern>%contextName %d{"yyyy-MM-dd HH:mm:ss,SSS"} [%thread] [%-5p] [%c{0}-%M] - %m%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- root节点：
            是必选节点，用来指定最基础的日志输出级别，只有一个level属性。-->
    <!--<root level="debug">
        <appender-ref ref="console" />
        <appender-ref ref="infoLogFile" />
        <appender-ref ref="errorLogFile" />
    </root>-->
    <logger name="com.greentech.kmcg" level="DEBUG" additivity="false">
        <appender-ref ref="console"/>
        <appender-ref ref="infoLogFile"/>
        <appender-ref ref="errorLogFile"/>
    </logger>
</configuration>